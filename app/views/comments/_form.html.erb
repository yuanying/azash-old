<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class='property_name'><%= f.label :name %></div>
    <div class='property_value'><%= f.text_field :name %></div>
  </div>
  <div class="field">
    <div class='property_name'><%= f.label :email %></div>
    <div class='property_value'><%= f.text_field :email %></div>
  </div>
  <div class="field">
    <div class='property_name'><%= f.label :url %></div>
    <div class='property_value'><%= f.text_field :url %></div>
  </div>
  <div class="field">
    <div class='property_name'><%= f.label :body_text %></div>
    <div class='property_value'><%= f.text_area :body_text %></div>
  </div>
  <div class="field">
    <div class='property_name'><%= f.label :entry %></div>
    <div class='property_value'><%= f.text_field :entry %></div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
