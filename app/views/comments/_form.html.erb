<%= form_for(@comment, :url => comments_url(:site_id => params[:site_id], :path => params[:path] ) ) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field required">
    <div class='property_name'><%= f.label :name %></div>
    <div class='property_value'><%= f.text_field :name %></div>
  </div>
  <div class="field">
    <div class='property_name'><%= f.label :email %></div>
    <div class='property_value'><%= f.text_field :email %></div>
  </div>
  <div class="field">
    <div class='property_name'><%= f.label :url %></div>
    <div class='property_value'><%= f.text_field :url %></div>
  </div>
  <div class="field required">
    <div class='property_name'><%= f.label :content %></div>
    <div class='property_value'><%= f.text_area :content %></div>
  </div>
  <div class="actions">
    <%= f.submit t('activerecord.models.comment') %>
  </div>
<% end %>
